using UnityEngine;

public enum WeaponUpgradeTypes
{
    damage,
    fireRate,
    range,
    knockback,
    projectileSpeed
}

public enum PlayerUpgradeTypes
{
    movementSpeed,
    minePower,
    maxHealth
}
